# Apartment wall textures

Upload the seamless wall base-colour texture here using this exact filename:

`plaster_wall_basecolor.png`

Reserved derived texture filenames:

- `plaster_wall_bump.png`
- `plaster_wall_roughness.png`

The bump and roughness maps will be derived in code after the base-colour image is uploaded.
