# Apartment floor textures

Upload the seamless generated base-colour texture here using this exact filename:

`oak_floor_basecolor.png`

Reserved derived texture filenames:

- `oak_floor_bump.png`
- `oak_floor_roughness.png`

The bump and roughness maps will be generated after the base-colour image is uploaded and inspected. Do not upload placeholder image files.
