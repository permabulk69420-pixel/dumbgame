# Apartment ceiling textures

Upload the seamless ceiling base-colour texture here using this exact filename:

`plaster_ceiling_basecolor.png`

Reserved derived texture filenames:

- `plaster_ceiling_bump.png`
- `plaster_ceiling_roughness.png`

The bump and roughness maps will be derived in code after the base-colour image is uploaded.
